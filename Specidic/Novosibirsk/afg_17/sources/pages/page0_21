<?php
{*text0_5*}
{*text0_6*}
{*text0_21*}
{*text0_22*}
{*text0_23*}
// МАТЕРИАЛЫ
$txt=@file_get_contents($pb_root.'sys/materials'); $mtr=false; $mtr[]=false; for ($i=1; $i<351; $i++) {
    $pos=strpos($txt,chr(10)); if ($pos==false) break; $str=substr($txt,0,$pos); $txt=substr($txt,$pos+1);
    if ($str!=' ') $mtr[]=explode(chr(9),$str); // $mtr[$i][1]=preg_replace('/\%/','',$mtr[$i][1]);
    else $mtr[]=false;
}
$txt=@file_get_contents($pb_root.'sys/materials_new'); $mtrt=false; $mtrt[]=false; for ($i=1; $i<351; $i++) {
    $pos=strpos($txt,chr(10)); if ($pos==false) break; $str=substr($txt,0,$pos); $txt=substr($txt,$pos+1);
    if ($str!=' ') $mtrt[]=explode(chr(9),$str);
    else $mtrt[]=false;
}
$txt=@file_get_contents($pb_root.'sys/materials_dop'); $mtr_dop=false; $mtr_dop[]=false; for ($i=1; $i<351; $i++) {
    $pos=strpos($txt,chr(10)); if ($pos==false) break; $str=substr($txt,0,$pos); $txt=substr($txt,$pos+1);
    if ($str!=' ') $mtr_dop[]=explode(chr(9),$str); 
    else $mtr_dop[]=false;
}

$txt=@file_get_contents($pb_root.'sys/names'); $mtr_names=false; $mtr_names[]=false; for ($i=1; $i<351; $i++) { 
    $pos=strpos($txt,chr(10)); if ($pos==false) break; $str=substr($txt,0,$pos); $txt=substr($txt,$pos+1);
    if ($str!=' ') $mtr_names[]=explode(chr(9),$str); 
    else $mtr_names[]=false;
}

// ПОКРЫТИЯ
$txt=@file_get_contents($pb_root.'sys/covers'); $pkr=false; $pkr[]=false; for ($i=1; $i<221; $i++) {
    $pos=strpos($txt,chr(10)); if ($pos==false) break; $str=substr($txt,0,$pos); $txt=substr($txt,$pos+1);
    // $str=str_replace('%','',substr($txt,0,$pos)); $txt=substr($txt,$pos+1);
    if ($str!=' ') $pkr[]=explode(chr(9),$str); else $pkr[]=false;
}
$txt=@file_get_contents($pb_root.'sys/covers_dop'); $pkr_dop=false; $pkr_dop[]=false; for ($i=1; $i<351; $i++) {
    $pos=strpos($txt,chr(10)); if ($pos==false) break; $str=substr($txt,0,$pos); $txt=substr($txt,$pos+1);
    if ($str!=' ') $pkr_dop[]=explode(chr(9),$str); 
    else $pkr_dop[]=false;
}
{*text2_9*}//переназначение цен на доп. материалы

?>
