    var y = Math.pow(pi_so, -0.357)*11.77606;
    if (pi_so>=1000) y = 1;
    if (pi_so>=2000) y = 0.95;
    if (pi_so>=5000) y = 0.9;
    if (pi_so>=10000) y = 0.85;
    if (pi_so>=20000) y = 0.8;
    return y;
